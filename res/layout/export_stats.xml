<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/LinearLayout01"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#aa000000"
    android:gravity="center"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="@drawable/app_background"
        android:gravity="center"
        android:orientation="vertical">

        <ListView
            android:id="@+id/list_view_id"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_above="@+id/export_dialog_buttons"
            android:layout_centerInParent="true"
            android:layout_margin="2dp"
            android:cacheColorHint="#fff"
            android:choiceMode="none"
            android:divider="#fff"
            android:dividerHeight="2sp"
            android:persistentDrawingCache="animation|scrolling" />


        <LinearLayout
            android:id="@+id/export_dialog_buttons"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:orientation="horizontal">


            <Button
                android:id="@+id/export_dialog_close_button"
                style="@style/MainTableText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_margin="5dp"
                android:layout_weight="1"
                android:background="@drawable/image_button"
                android:padding="5dp"
                android:text="@string/cancel"
                android:textColor="#fff"
                android:textStyle="bold" />

            <!--As of Android 4.0 the positive button has to be
            on the right hand side-->
            <Button
                android:id="@+id/export_dialog_export_button"
                style="@style/MainTableText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_margin="5dp"
                android:layout_weight="1"
                android:background="@drawable/image_button"
                android:padding="5dp"
                android:text="@string/run_export"
                android:textColor="#fff"
                android:textStyle="bold" />

        </LinearLayout>
    </RelativeLayout>
</LinearLayout>